---
title: 内存模型
description: 整理 C++ 对象在内存中的存储、对齐与布局规则，以及这些规则和底层硬件之间的关系。
publish: false
---

# 内存模型

这里记录 C++ 对象在内存中的组织方式，关注语言规则如何落到实际的存储布局和内存访问上。

## 主要内容

- 类型大小、对齐要求与填充字节
- 对象布局和成员在内存中的排列
- `sizeof`、`alignof` 与对齐控制
- 内存访问规则和底层硬件之间的关系

可以从左侧目录进入对应笔记。
